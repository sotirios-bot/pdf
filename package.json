{
  "name": "pdfconvertme",
  "version": "0.1.0",
  "private": true,
  "description": "PDFConvertMe — online PDF tools, starting with PDF to Excel. Multilingual (EN/RU/TR), built to grow. Operated by XYZ LAB PTE. LTD.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node scripts/serve.js",
    "preview": "node build.js && node scripts/preview.js",
    "clean": "rm -rf public previews"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "docx": "^9.7.1",
    "exceljs": "^4.4.0",
    "pdfjs-dist": "^3.11.174"
  }
}
